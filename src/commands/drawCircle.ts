//import { Button, mouse } from "@nut-tree/nut-js";

export const drawCircle = async(args: string[]) => {
    const radius = Number(args[0]);

    console.log(`Circle is drawn with radius - ${radius}`)
}
